---
aside: true
---

# Конфиг мода

Конфиг мода находится в файле `MPG_Spawner/Config.json` внутри папки с профилем сервера.

## configVersion

`configVersion` - Версия конфига, служебное поле, не нужно его менять. Оно обновляется само при обновлении мода, если в
этом есть необходимость.

## isModEnabled

`isModDisabled` - Включение или отключение мода целиком.

## isDebugEnabled

`isDebugEnabled` - Включение или отключение дебага мода. При включении дебага в логи будет сыпаться много разной
информации, полезной для отладки всех точек.

## pointsConfigs

`pointsConfigs` - Массив с названиями json файлов с конфигами точек в папке Points из которых будет загружена информация
для создания
триггеров.

Пример:

```
"pointsConfigs": [
  "Point_Example1",
  "Point_Example2"
]
```
